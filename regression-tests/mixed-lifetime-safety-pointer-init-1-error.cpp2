
#include <iostream>
#include <random>

main: () -> int = {
    x: int = 42;
    p: *int;

    // ... more code ...
    // the standard mandates that std::mt19937()() == 3499211612
    if std::mt19937()()%2 {
        // p = nullptr;
    }
    else {
        // p = x&;
    }

    print_and_decorate( p* );
}

print_and_decorate: (thing:_) =
    std::cout << ">> " << thing << "\n";

