f: <T> () -> decltype(T().a()) = { } // Fails on Clang 12 (lambda in unevaluated context).

B: type = { }

main: () = {
  g := :<T> (_: T) -> decltype(f<T>()) = {};
  static_assert(!std::is_invocable_v<decltype(g), B>);
}
